apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clustering-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: clustering
                port: 
                  number: 80