apiVersion: v1
kind: Service
metadata:
  name: clustering-service-loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: clustering-$COLOR_ACTIVE
    color: $COLOR_ACTIVE
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80